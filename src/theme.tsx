import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#002279",
    },
    secondary: {
      main: "#19857b",
    },
    error: {
      main: "#EB421C",
    },
  },
});

export default theme;
