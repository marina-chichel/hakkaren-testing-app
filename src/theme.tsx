import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#cab595",
      "900": "#333",
    },
    secondary: {
      main: "#FFF",
    },
    error: {
      main: "#EB421C",
    },
  },
});

export default theme;
